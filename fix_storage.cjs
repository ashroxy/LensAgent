const fs=require("fs"); let code=fs.readFileSync("lib/storage.js","utf8"); code=code.replace(/return \{ ...DEFAULT_SETTINGS, ...saved \};/, "const finalSettings = { ...DEFAULT_SETTINGS }; for (const k in saved) { if (saved[k] !== \"\" && saved[k] !== null && saved[k] !== undefined) finalSettings[k] = saved[k]; } return finalSettings;"); fs.writeFileSync("lib/storage.js",code,"utf8");
